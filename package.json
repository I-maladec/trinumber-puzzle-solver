{
  "name": "trinumber-puzzle-solver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --format=iife --outfile=main.js && esbuild src/scripts/solverWorker.ts --bundle --format=iife --outfile=solverWorker.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^7.0.2"
  }
}
